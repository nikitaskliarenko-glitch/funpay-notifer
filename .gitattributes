# Снимки живых страниц FunPay — фикстуры тестов. Переводы строк в них менять
# нельзя, иначе на другой машине разбор поедет вместе с содержимым.
tests/fixtures/**/*.html -text
*.py text eol=lf
*.md text eol=lf
